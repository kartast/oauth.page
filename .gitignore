node_modules/
.wrangler/

# Secrets — never commit
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
.env.credentials
*.pem
*.key

# Cloudflare deployment config — unique to each account (use the .example)
worker/wrangler.toml
!worker/wrangler.toml.example

# Build output
dist/

# OS / editor
.DS_Store
*.log
